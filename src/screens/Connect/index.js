import React from 'react';
import { View, Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const Connect = () => {

  return (
    <SafeAreaView>
      <Text>
        Connect Screen: where you will message an instructor and ask questions about training
      </Text>
    </SafeAreaView>
  )
}

export default Connect;